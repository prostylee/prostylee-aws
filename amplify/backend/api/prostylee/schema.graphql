type Comment @model @auth(
    rules: [
      { allow: owner, operations: [create, delete, update] }
    ]) 
    @key(name: "ByParent", fields: ["parentId"]) {
   id: ID!
   ownerId: String!
   owner: String!
   targetId: String!
   targetType: String!
   content: String!
   numberOfLikes: Int
   createdAt: AWSDateTime!
   parentId: ID!
   parent: Comment @connection(fields: ["parentId"])
   childrens: [Comment] @connection(keyName: "ByParent", fields: ["id"])
}